import CommandLine from "../CommandLine";

const DefaultInstructions = () => {
  return (
    <>
      <code className="cta code">
        <CommandLine>npm i -g @deepgram/cli</CommandLine>
      </code>
    </>
  );
};

export default DefaultInstructions;
